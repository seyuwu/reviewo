{
  "name": "@reviewo/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared technical utilities for the Reviewo monorepo.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "tsc -p tsconfig.json && tsc -p tsconfig.test.json && node --test dist-test/entity-chat-messages.test.js dist-test/compare-slug.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.0.0"
  }
}
