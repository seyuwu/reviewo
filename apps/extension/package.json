{
  "name": "@reviewo/extension",
  "version": "0.0.0",
  "private": true,
  "description": "Reviewo browser extension.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "lint": "eslint .",
    "test": "node scripts/build.mjs && node scripts/verify-build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "esbuild": "^0.27.3"
  }
}
